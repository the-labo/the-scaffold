'use strict'

import c from 'classnames'
import PropTypes from 'prop-types'
import React from 'react'
import { eventHandlersFor, htmlAttributesFor } from 'the-component-util'

/**
 * ~~~~package_description~~~~
 */
class ~~~~package_name@pascalcase~~~~ extends React.Component {
  render () {
    const { props } = this
    const {
      children,
      className,
    } = props
    return (
      <div {...htmlAttributesFor(props, { except: ['className'] })}
           {...eventHandlersFor(props, { except: [] })}
           className={c('~~~~package_name~~~~', className)}
      >
        {children}
      </div>
    )
  }
}

~~~~package_name@pascalcase~~~~.propTypes = {}

~~~~package_name@pascalcase~~~~.defaultProps = {}

~~~~package_name@pascalcase~~~~.displayName = '~~~~package_name@pascalcase~~~~'

export default ~~~~package_name@pascalcase~~~~
