'use strict'

const theDb = require('the-db')
const { ~~~~name_of_resource_class~~~~ } = require('~~~~package_name~~~~')

async function tryExample () {
  let db = theDb({
    dialect: 'memory',
    resources: {
      Example: class ExampleResource extends ~~~~name_of_resource_class~~~~ {
        static get nameString () {
          return 'Example'
        }
      }
    }
  })
}

tryExample().catch((err) => console.error(err))
