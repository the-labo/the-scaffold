/**
 * Create a SomeResource instance
 * @function create
 * @param {...*} args
 * @returns {SomeResource}
 */
'use strict'

const SomeResource = require('./SomeResource')

/** @lends create */
function create (...args) {
  return new SomeResource(...args)
}

module.exports = create
