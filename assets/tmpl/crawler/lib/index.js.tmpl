/**
 * ~~~~package_description~~~~
 * @module ~~~~package_name~~~~
 */
'use strict'

const ~~~~name_of_crawler_class~~~~ = require('./~~~~name_of_crawler_class~~~~')
const create = require('./create')

const lib = create.bind(this)

Object.assign(lib, {
  ~~~~name_of_crawler_class~~~~,
  create
})

module.exports = lib
